<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appagendamento"
        tools:targetApi="31">

        <activity
            android:name=".TelaDeslogar"
            android:exported="false" />
        <activity
            android:name=".Cadastro"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen"/>

        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen">
        </activity>

        <activity
            android:name=".activities.CategoryActivity"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity"/>
        </activity>

        <activity
            android:name=".activities.ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen"
            android:parentActivityName=".activities.CategoryActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.CategoryActivity" />
        </activity>

        <activity
            android:name=".activities.CartActivity"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <activity
            android:name=".activities.AddProductActivity"
            android:exported="false"
            android:theme="@style/Theme.Appagendamento.Fullscreen"
            android:parentActivityName=".activities.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>


        <activity
            android:name=".FormLogin"
            android:exported="true"
            android:theme="@style/Theme.Appagendamento.Fullscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>